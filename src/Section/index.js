import Form from "../Form";
import Header from "../Header";
import "./style.css";

const Section = () => {

    return (
        <section className="conventer">
            <Header />
            <Form />
        </section>
    )
};

export default Section;